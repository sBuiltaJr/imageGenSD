{
	"table_fmt" : "IGSDEconomy (u_ID BIGINT UNSIGNED NOT NULL UNIQUE PRIMARY KEY, builder_count INT UNSIGNED NOT NULL, builder_level INT UNSIGNED NOT NULL, builder_limit INT UNSIGNED NOT NULL, crafter_count INT UNSIGNED NOT NULL, crafter_level INT UNSIGNED NOT NULL, crafter_limit INT UNSIGNED NOT NULL, hospital_count INT UNSIGNED NOT NULL, hospital_level INT UNSIGNED NOT NULL, hospital_limit INT UNSIGNED NOT NULL, keygen_count INT UNSIGNED NOT NULL, keygen_level INT UNSIGNED NOT NULL, keygen_limit INT UNSIGNED NOT NULL, research_count INT UNSIGNED NOT NULL, research_level INT UNSIGNED NOT NULL, research_limit INT UNSIGNED NOT NULL, team_count INT UNSIGNED NOT NULL, team_level INT UNSIGNED NOT NULL, team_limit INT UNSIGNED NOT NULL, worker_count INT UNSIGNED NOT NULL, worker_level INT UNSIGNED NOT NULL, worker_limit INT UNSIGNED NOT NULL);"
}